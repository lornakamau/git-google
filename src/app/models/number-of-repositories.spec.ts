import { NumberOfRepositories } from './number-of-repositories';

describe('NumberOfRepositories', () => {
  it('should create an instance', () => {
    expect(new NumberOfRepositories()).toBeTruthy();
  });
});
