import { VisitedRepositoryDirective } from './visited-repository.directive';

describe('VisitedRepositoryDirective', () => {
  it('should create an instance', () => {
    const directive = new VisitedRepositoryDirective();
    expect(directive).toBeTruthy();
  });
});
