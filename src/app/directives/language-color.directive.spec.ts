import { LanguageColorDirective } from './language-color.directive';

describe('LanguageColorDirective', () => {
  it('should create an instance', () => {
    const directive = new LanguageColorDirective();
    expect(directive).toBeTruthy();
  });
});
